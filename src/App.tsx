import { AppRouter } from './routes/Routes';

function App() {
  return (
    <div className=' fixed w-full h-screen bg-gradient-to-r from-blue-500'>
      <AppRouter />
    </div>
  );
}

export default App;
