import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#e1f5fe',
    },
    secondary: { main: 'rgba(255, 0, 0, 0.1)' },
    info: { main: '#3b82f6' },
    success: { main: '#3b82f6' },
  },
});
